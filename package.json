{
  "name": "your-side",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.21.4",
    "dotenv": "^16.4.5"
  },
  "scripts": {
    "login:server": "npx supabase login",
    "start:supabase": "npx supabase start",
    "start:server": "npx supabase functions serve",
    "start:deploy": "npx supabase functions deploy"
  },
  "devDependencies": {
    "supabase": "^2.0.0"
  }
}
